export interface MoveDeviceToZone {
    deviceId: string;
    zoneId: number;
}

export interface NameDevice {
    deviceId: string;
    name: string;
}

export interface NameZone {
    zoneId: number;
    name: string;
}
